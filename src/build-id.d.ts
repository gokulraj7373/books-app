/**
 * Replaced at build time by `define` in vite.config.ts — see the comment there
 * for why this exists. Declared here rather than in vite-env.d.ts so the reason
 * travels with the symbol.
 */
declare const __BUILD_ID__: string;
